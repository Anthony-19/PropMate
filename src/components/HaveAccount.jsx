
export const HaveAccount = (props) =>{
    
    return(
        // <p className="have-account">{props.label}</p>
        <>
        <p className="have-account">{props.label}</p>
        </>
    
    
    )
}